apiVersion: v2
name: helm-chart-test-oci
description: A Helm chart for Kubernetes
type: application
version: "0.0.0"
appVersion: "1.16.0"

dependencies:
- name: redis
  version: "20.6.0"
  repository: "https://charts.bitnami.com/bitnami"
